const firstName = 'John';
const age = 35;
const hobby = 'Coding';

const logTwice = () => {
  console.log(parameter);
  console.log(parameter);
};

const hobbyfunction = () => {
  logTwice(`Hello, ${firstname} (${age}). I love ${hobby}!`);
};

hobbyFunction();